# AI 求职工作台交接文档

这份文档是当前项目的唯一交接入口。  
新开的每个线程窗口都必须先读这份文档，再开始工作。

---

## 1. 项目当前状态

当前项目已经不是从 0 到 1 的原型阶段，而是：

- 已有可登录、可使用、已部署的 Web 产品
- 正式地址：`https://project-iry1g.vercel.app`
- 当前重点不是再铺大功能，而是：
  - 收口现有功能质量
  - 修正文链路与微调链路
  - 稳定导出与展示体验
  - 收紧 UI 与部署稳定性

当前已经可用的主功能：

- 账号登录 / 会话
- 岗位工作台
- 岗位导入与解析
- 岗位详情编辑
- 简历仓库
- 原始简历与源文件资产
- 简历微调
- 通知管理
- Agent API / Token / Webhook 第一阶段
- 已部署到 Vercel

---

## 2. 已完成内容

这些内容已经完成，不要在新线程里再把它们当成主任务重复做：

- 岗位工作台已经替代“岗位列表 + 申请看板”
- 简历仓库已经支持原始简历、源文件资产、定制版本
- 账户中心已经支持用户级 AI 配置
- Agent 接口第一阶段已经完成：
  - API Token
  - webhook 配置
  - 5 个入站 API
  - Agent Run
  - 文档页
- 项目已能部署到 Vercel
- `DOCX` 正文提取已经基本恢复
- 简历微调按钮已有 loading
- Tailor 顶部旧的小标记已经去掉
- Jobs 工作台已经做过多轮紧凑化

不要再把上面这些当成“待实现需求”。

---

## 3. 当前最重要的未完成问题

### A. 简历正文可信度还没有彻底收口

现象：

- 微调页里的“原始版本”一度显示前后颠倒或旧错正文
- `DOCX` 当前抽样本身顺序看起来正常
- 更像是数据库里残留过旧的 `resume.rawText`
- 现在虽然已经把微调时的优先级改成“先读资产 extractedText，再回退 rawText”，但这条链路还需要彻底收口

结论：

- 现在最需要的是统一：
  - `Resume.rawText`
  - `ResumeAsset.extractedText`
  - 微调页展示原文
  - 生成草稿使用原文

### B. 简历微调逻辑还不稳定

现象：

- 用户想要的是：
  - 保留原始简历结构
  - 只微调每一部分的表述
  - 更贴合 JD
- 当前草稿质量、结构保留、迭代体验还不够稳
- “微调建议”与“完整草稿”的边界要更清楚

### C. 导出文件仍然不够像正式简历

现象：

- 导出的 docx / pdf 更接近结构化文本
- 不是正式简历模板排版
- 还没有个人照片这一层

### D. PDF 正文提取仍不稳定

现象：

- `DOCX` 基本恢复
- `PDF` 重新提取正文仍然不稳定
- 这条链路现在不是最高优先级，可以降级处理

### E. UI 还需要继续收口

现象：

- Jobs 表格还在持续微调紧凑度
- Tailor / Resumes / Account / Agent docs 还有细节体验可继续打磨

### F. 上线入口仍然是 `vercel.app`

现象：

- 产品已上线，但目前正式入口还是 `project-iry1g.vercel.app`
- 中国大陆网络下访问仍可能不稳定
- 自定义域名 + Cloudflare 方案还没正式实施

---

## 4. 目录与关键文件

### 核心目录

- `app/`
- `components/`
- `lib/`
- `prisma/`

### 当前高频修改文件

- `app/tailor/page.tsx`
- `app/resumes/page.tsx`
- `app/jobs/page.tsx`
- `app/jobs/[id]/page.tsx`
- `components/jobs-list-client.tsx`
- `components/tailor-start-form.tsx`
- `components/tailor-pending.tsx`
- `app/actions.ts`
- `lib/ai.ts`
- `lib/uploads.ts`

### 数据层重点模型

- `JobLead`
- `Application`
- `Resume`
- `ResumeAsset`
- `ResumeVariant`
- `ResumeTailorRun`
- `Event`
- `ApiToken`
- `AgentRun`

---

## 5. 所有线程都必须先知道的事实

1. 这个项目已经在线上跑着，不能把线程任务当成从头重构。
2. 不要重复实现已经完成的功能。
3. 每个线程都要尽量缩小改动面，避免互相打架。
4. Tailor、Resumes、Actions 是当前最容易产生冲突的区域。
5. 如果线程要改 `app/actions.ts`，一定只改自己负责的函数，并在交付时写清楚改了哪些 action。
6. 如果线程要改 `lib/ai.ts`，一定说明：
   - 改的是建议链路还是草稿链路
   - 改的是 prompt、schema 还是响应解析
7. 如果线程要改数据库 schema，不要轻易动线上已稳定字段，除非这个线程明确负责数据模型演进。
8. 如果线程发现任务已偏离当前主线，要主动收回来，不要继续功能膨胀。

---

## 6. 推荐并行方案

建议先开 4 个线程，等前两条稳定后再开第 5 条：

### 线程 1：简历正文可信度

目标：

- 修正原始版本显示错乱
- 统一 `Resume.rawText` 与 `ResumeAsset.extractedText`
- 增加“把当前编辑源正文同步到主版本”的能力
- 保证微调页、草稿生成、建议展示读取的是同一份正确正文

### 线程 2：简历微调产品逻辑

目标：

- 微调建议只保留结构化分析
- 完整草稿只负责生成完整简历草稿
- 草稿迭代稳定
- 最大限度保留原始简历分区结构，只优化每部分表达

### 线程 3：导出模板化

目标：

- 让导出的 docx / pdf 更像正式简历
- 固定分区结构和排版层级
- 为后续照片接入预留结构

### 线程 4：工作台与整体 UI 收口

目标：

- 继续压工作台信息密度
- 收紧 Tailor / Resumes / Account 的页面观感
- 统一按钮、留白、输入框高度、文案

### 线程 5：部署与稳定性

目标：

- 自定义域名 + Cloudflare 接入准备
- 线上稳定性盘点
- 监控、错误日志、上线清单

建议顺序：

1. 线程 1
2. 线程 2
3. 线程 4
4. 线程 5
5. 线程 3 最后做

原因：

- 线程 1 和 线程 2 会直接影响微调质量，是当前最核心问题
- 线程 3 依赖前面的草稿结构更稳定后再做更划算

---

## 7. 每个线程窗口的提示词

### 线程 1：简历正文可信度

把下面这段发给新窗口：

```text
请先阅读根目录的 `job-hunt-ai-workbench-handover.md`，然后只负责“简历正文可信度”这一条线，不要改别的页面功能。

目标：
1. 查清为什么微调页“原始版本”会显示旧错正文或顺序错乱
2. 统一 Resume.rawText 和 ResumeAsset.extractedText 的优先级
3. 增加一个“把当前编辑源正文同步到主版本”的能力
4. 优先修好 DOCX 正文链路；PDF 如果仍不稳，只保留降级提示，不要继续深挖
5. 改完后明确说明：简历仓库、微调建议、完整草稿分别使用哪份正文

重点检查：
- app/actions.ts
- app/resumes/page.tsx
- app/tailor/page.tsx
- lib/uploads.ts

要求：
- 不要改工作台 UI
- 不要改 Agent
- 只做正文链路和数据一致性
```

### 线程 2：简历微调产品逻辑

把下面这段发给新窗口：

```text
请先阅读根目录的 `job-hunt-ai-workbench-handover.md`，然后只负责“简历微调产品逻辑收口”，不要碰正文同步、导出模板和部署。

目标：
1. 微调建议页面只显示：
   - 匹配分析
   - JD 解析
   - 简历素材拆解
   - 风险校验
2. 删除旧的气泡式微调建议
3. 删除四个具体分数，只保留：
   - 优势项
   - 差距项
   - 优先调整点
4. 完整草稿只负责输出完整简历草稿
5. 草稿迭代必须尽量保留原始简历分区结构
6. 禁止第一人称
7. 修掉生成、迭代、展示之间的交互不稳定问题

重点检查：
- app/tailor/page.tsx
- components/tailor-start-form.tsx
- components/tailor-pending.tsx
- app/actions.ts
- lib/ai.ts

要求：
- 不要碰 PDF 提取
- 不要碰工作台
- 不要做导出模板大改
```

### 线程 3：导出模板化

把下面这段发给新窗口：

```text
请先阅读根目录的 `job-hunt-ai-workbench-handover.md`，然后只负责“导出模板化”，不要改微调建议逻辑和正文提取。

目标：
1. 让导出的 DOCX 更像正式简历，不是纯文本
2. 固定几个分区：
   - 基本信息
   - 个人简介
   - 教育经历
   - 实习经历
   - 项目经历
   - 技能/优势
3. 分区标题、留白、段落层次要明显
4. 暂时不要求完全复刻用户原模板
5. 如果照片暂时接不进去，要明确保留接口和后续接法

重点检查：
- lib/docx-export.ts
- lib/pdf-export.ts
- app/actions.ts 中导出归档逻辑

要求：
- 不要改微调页整体 UI
- 不要改 Agent
- 先把排版做成稳定模板
```

### 线程 4：工作台与整体 UI 收口

把下面这段发给新窗口：

```text
请先阅读根目录的 `job-hunt-ai-workbench-handover.md`，然后只负责“岗位工作台和整体 UI 收口”，不要碰 AI 逻辑和正文提取。

目标：
1. 继续减少不必要滑动
2. 优化工作台列宽、行高、输入框高度
3. 修零散文案和视觉不统一问题
4. 统一高频页面的按钮尺寸、留白和信息密度
5. 不改核心数据逻辑

重点检查：
- components/jobs-list-client.tsx
- app/jobs/page.tsx
- app/jobs/[id]/page.tsx
- app/resumes/page.tsx
- components/app-shell.tsx

要求：
- 不要动微调工作流
- 不要动导出
- 不要动 Agent API
```

### 线程 5：部署与稳定性

把下面这段发给新窗口：

```text
请先阅读根目录的 `job-hunt-ai-workbench-handover.md`，然后只负责“上线与稳定性收口”，不要改产品主功能。

目标：
1. 盘点当前 Vercel 生产环境
2. 梳理自定义域名 + Cloudflare 的最小实施方案
3. 盘点当前线上风险：
   - vercel.app 国内访问不稳
   - 数据库连接波动
   - 缺少错误监控
4. 输出一份上线清单和域名切换清单
5. 如果代码里缺少监控入口，可以补最小方案

要求：
- 尽量少改产品页面
- 重点是文档、部署、运维侧收口
```

---

## 8. 当前建议

你现在去开新窗口时，先开这 4 个：

- 线程 1：简历正文可信度
- 线程 2：简历微调产品逻辑
- 线程 4：工作台与整体 UI 收口
- 线程 5：部署与稳定性

等线程 1 和 线程 2 结论稳定之后，再开：

- 线程 3：导出模板化

这样冲突最少，也最符合当前主线。
